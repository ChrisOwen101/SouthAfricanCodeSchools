language: node_js
node_js:
  - "7"
script:
  - echo "Deploy!!"
cache: npm
install:
  - npm install -g firebase-tools
  - npm install
  - npm update
  - npm run build
after_success:
  - firebase deploy --token $firebase_key
